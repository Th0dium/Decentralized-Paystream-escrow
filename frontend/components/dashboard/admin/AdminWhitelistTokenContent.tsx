"use client";

import { Card } from "@/components/Card";

export default function AdminWhitelistTokenContent() {
  return (
    <Card>
      <h2 className="text-xl font-semibold mb-4">Admin: Whitelist Token</h2>
      <p className="text-slate-300">
        This is where the content for whitelisting tokens for an admin will go.
        (Original content from frontend/app/dashboard/admin/whitelist-token/page.tsx needs to be inserted here.)
      </p>
    </Card>
  );
}